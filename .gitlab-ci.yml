stages:
  - deploy-public

default:
  image: ubuntu:20.04

deploy-heroku:
  stage: deploy-public
  tags:
    - deployment
  script:
    - apt-get update -yq
    - apt-get install -y ruby-dev
    - gem install dpl
    - dpl --provider=heroku --app=gpqueue-year-2-web-app --api-key=$HEROKU_TOKEN

