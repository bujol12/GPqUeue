build:
  docker:
    backend: backend/Dockerfile
    frontend: frontend/Dockerfile
run:
  backend:
    command:
      - sh backend/start_dev.sh
    image: backend
  frontend:
    command:
      - cp -r ./frontend/src /app/src
      - cp -r ./frontend/public /app/public
      - npm start
    image: frontend

